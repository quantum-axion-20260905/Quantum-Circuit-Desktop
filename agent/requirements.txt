fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
# GPU (CUDA) - local compute
cupy-cuda12x
# CUDA DLLs (Windows-friendly pip packages)
nvidia-cublas-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12
# Tensor network stack (CPU/GPU via CuPy backend)
opt_einsum
cotengra
autoray
quimb
# Quantum stack (install later as needed; keep optional for now)
# qiskit
# qiskit-aer
