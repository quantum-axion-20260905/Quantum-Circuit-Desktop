# Schemas

Bu papkada “contracts” turadi:

- Circuit IR schemas (QASM3 wrapper + UI metadata)
- Agent compute job schemas (payload/response)

Maqsad: UI/agent/backend bir-biriga yopishib qolmasligi uchun bitta aniq contract bo‘lsin.

